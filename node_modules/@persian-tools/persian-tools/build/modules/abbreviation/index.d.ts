export declare const abbreviateNumber: (num: number, { digit, padding, symbols }?: AbbreviateOptions) => string;
export declare const expandNumber: (num: string, symbols?: AbbreviateOptions["symbols"]) => number;
export interface AbbreviateOptions {
    digit?: number;
    padding?: boolean;
    symbols?: string[];
}
