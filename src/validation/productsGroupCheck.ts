import { isValidObjectId } from "mongoose";
import { ProductModel } from "../models/productModel";

export const checkProductGroup = async(productsId: string[],discount:number,time:number,remainderCcount:number)  => {
}
